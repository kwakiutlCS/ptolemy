<% @activities.each do |a| %>
    <div class="ptolemy_teacher_board_cell">
      <div>
	 <b><%= link_to a.template.title, a, data:{activity: a.id} %></b><% if a.description != "" %> - <%= a.description %> <% end %>
	 - <% if a.inactive %><%= link_to "activar", switch_activity_path(a.id), remote: true %><% else %><%= link_to "desactivar", switch_activity_path(a.id), remote: true %><% end %>
      </div>
      <div>
	 código: <%= a.code %><% if a.deadline %> - limite: <%= a.deadline.strftime("%d-%m-%Y") %> <% end %> - respostas: <%= a.answers.submited.count %>
      </div>
    </div>
    <% end %>
